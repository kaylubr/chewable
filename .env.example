# Backend environment (loaded by core/ via pydantic-settings)
# Local Postgres comes from `docker compose up -d` — see compose.yml.
DATABASE_URL=postgresql+asyncpg://chewables:chewables@localhost:5432/chewables
